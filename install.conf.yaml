- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.functions: functions
    ~/.aliases: aliases
    ~/.environment: environment
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh
    ~/.vim: 
        glob: true
        path: vim/*
